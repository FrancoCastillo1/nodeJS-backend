<div class="bienvenida">
    <h2>Bievenido: {{user.firts_name}} {{user.last_name}}</h2>
</div>
<div>
    <h1>Ingresar Datos</h1>
    <form id="productForm">
        <div>
            <label for="title">Nombre del Producto</label>
            <input type="text" name="title" id="title" />
        </div>
        <div>
            <label for="description">Descripcion</label>
            <input type="text" name="description" id="description" />
        </div>
        <div>
            <label for="price">Precio</label>
            <input type="text" name="price" id="price" />
        </div>
        <div>
            <label for="stock">Stock</label>
            <input type="text" name="stock" id="stock" />
        </div>
        <div>
            <label for="category">Category</label>
            <input type="text" name="category" id="category" />
        </div>
        <div>
            <label for="thumbnail">Ingrese la imagen</label>
            <input type="text" name="thumbail" id="thumbnail" />
        </div>
        <button type="submit" id="btnProduct">Enviar</button>
    </form>
</div>

<h2>Productos:</h2>
<section class="sectionProducts">
    {{#each products}}
    <div class="products">
        <div class="info">
            <div class="header">
                <h2>Nombre del producto:{{this.title}}</h2>
                <p>id del producto:{{this._id}}</p> 
            </div>
            <div class="body">
                <p>Descripción:{{this.description}}</p>
            </div>
            <div class="footer">
                <p>precio:{{this.price}}</p>
                <p>staus:{{this.status}}</p>
                <p>stock:{{this.stock}}</p>
                <p>categoria:{{this.category}} </p>
            </div>
        </div>
        <div class="agregar">
          <button>Agregar al Carrito</button>
        </div>
    </div>
        {{/each}}
</section>
<script src="/socket.io/socket.io.js"></script>

